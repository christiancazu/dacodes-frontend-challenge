{
  "name": "@dacodes/directory",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -noEmit && vite build",
    "dev": "concurrently \"vite preview -l silent\" \"vite build --watch\"",
    "start": "vite preview"
  },
  "dependencies": {
    "@dacodes/lib": "workspace:*",
    "react-infinite-scroll-component": "^6.1.0"
  }
}
